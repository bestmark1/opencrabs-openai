[project]
name = "opencrabs-agent-base"
version = "0.1.0"
description = "Reusable Telegram support-agent base"
requires-python = ">=3.11"
dependencies = [
  "pydantic>=2.7",
  "pydantic-settings>=2.2",
  "httpx>=0.27",
  "telethon>=1.36",
]

[tool.setuptools]
py-modules = []
